/**
 * Enum representing the different user roles in the system.
 * This enum helps define the access levels for different types of users.
 * 
 * @readonly
 * @enum {string}
 */
export enum Role{
    "ADMIN" = "admin",
    "USER" = "user"
}